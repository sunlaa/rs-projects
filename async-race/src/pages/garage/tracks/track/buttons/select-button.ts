import BaseElement from '../../../../../utils/components/base-element';

export default class SelectButton extends BaseElement {
  constructor() {
    super({ className: ['select-button', 'button'] });
  }
}
